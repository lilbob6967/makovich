local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")

-- Settings
local REMOTE_PATH = ReplicatedStorage.Remotes.Functions.RequestTeamChange

-- Helper function to create border decorations (Lines and Dots)
local function createBorder(parent)
	local Border = Instance.new("Frame")
	Border.Name = "Border"
	Border.Size = UDim2.new(1, 0, 1, 0)
	Border.Position = UDim2.new(0, 0, 0, 0)
	Border.BackgroundTransparency = 1
	Border.BorderSizePixel = 1
	Border.BorderColor3 = Color3.fromRGB(27, 42, 53)
	Border.Parent = parent

	-- Create 4 Lines (edges)
	local linePositions = {
		UDim2.new(0, 0, 0, 0),           -- Top
		UDim2.new(0, 0, 1, -1),          -- Bottom
		UDim2.new(0, 0, 0, 0),           -- Left
		UDim2.new(1, -1, 0, 0)          -- Right
	}
	local lineSizes = {
		UDim2.new(1, 0, 0, 1),          -- Top
		UDim2.new(1, 0, 0, 1),          -- Bottom
		UDim2.new(0, 1, 1, 0),          -- Left
		UDim2.new(0, 1, 1, 0)           -- Right
	}

	for i = 1, 4 do
		local Line = Instance.new("Frame")
		Line.Name = "Line"
		Line.Size = lineSizes[i]
		Line.Position = linePositions[i]
		Line.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
		Line.BorderSizePixel = 0
		Line.Parent = Border
	end

	-- Create 4 Dots (corners)
	local dotPositions = {
		UDim2.new(0, 0, 0, 0),           -- Top-left
		UDim2.new(1, -2, 0, 0),         -- Top-right
		UDim2.new(0, 0, 1, -2),          -- Bottom-left
		UDim2.new(1, -2, 1, -2)          -- Bottom-right
	}

	for _, pos in ipairs(dotPositions) do
		local Dot = Instance.new("Frame")
		Dot.Name = "Dot"
		Dot.Size = UDim2.new(0, 2, 0, 2)
		Dot.Position = pos
		Dot.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		Dot.BorderSizePixel = 0
		Dot.Parent = Border
	end

	return Border
end

-- Create the UI Structure 1:1 from StarterGui.Screengui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Screengui"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true
ScreenGui.ScreenInsets = Enum.ScreenInsets.DeviceSafeInsets
ScreenGui.ClipToDeviceSafeArea = true
ScreenGui.SafeAreaCompatibility = Enum.SafeAreaCompatibility.FullscreenExtension
ScreenGui.Parent = Players.LocalPlayer:WaitForChild("PlayerGui")

-- Container Frame
local Container = Instance.new("Frame")
Container.Name = "Container"
Container.Size = UDim2.new(0, 300, 0, 155)
Container.Position = UDim2.new(0.462235659, 0, 0.785369754, 0)
Container.AnchorPoint = Vector2.new(0.5, 0.5)
Container.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Container.BackgroundTransparency = 0.7
Container.BorderSizePixel = 0
Container.BorderColor3 = Color3.fromRGB(27, 42, 53)
Container.ZIndex = 2
Container.Parent = ScreenGui

-- Container Border
createBorder(Container)

-- Title
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(1, 0, 0.163114607, 0)
Title.Position = UDim2.new(0, 0, 0, 2)
Title.BackgroundTransparency = 1
Title.Text = "Remote Team join"
Title.TextColor3 = Color3.fromRGB(255, 142, 12)
Title.TextSize = 34
Title.Font = Enum.Font.SourceSansSemibold
Title.TextWrapped = true
Title.TextXAlignment = Enum.TextXAlignment.Center
Title.TextYAlignment = Enum.TextYAlignment.Center
Title.ZIndex = 3
Title.Parent = Container

-- Description
local Description = Instance.new("TextLabel")
Description.Name = "Description"
Description.Size = UDim2.new(0.95, 0, 0.125, 0)
Description.Position = UDim2.new(0.025, 0, 0.215, 0)
Description.BackgroundTransparency = 1
Description.Text = "Note: Team must be owned"
Description.TextColor3 = Color3.fromRGB(255, 255, 255)
Description.TextSize = 20
Description.Font = Enum.Font.SourceSansSemibold
Description.TextWrapped = true
Description.RichText = true
Description.TextXAlignment = Enum.TextXAlignment.Center
Description.TextYAlignment = Enum.TextYAlignment.Top
Description.ZIndex = 3
Description.Parent = Container

-- Team TextBox
local TeamInput = Instance.new("TextBox")
TeamInput.Name = "Team"
TeamInput.Size = UDim2.new(1.0333333, -480, 0, 40)
TeamInput.Position = UDim2.new(0, 236, 0, 57)
TeamInput.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TeamInput.BackgroundTransparency = 0.6
TeamInput.BorderSizePixel = 0
TeamInput.TextColor3 = Color3.fromRGB(255, 255, 255)
TeamInput.PlaceholderText = "Team Name"
TeamInput.PlaceholderColor3 = Color3.fromRGB(178, 178, 178)
TeamInput.Text = ""
TeamInput.TextSize = 14
TeamInput.Font = Enum.Font.SourceSans
TeamInput.TextXAlignment = Enum.TextXAlignment.Center
TeamInput.TextYAlignment = Enum.TextYAlignment.Center
TeamInput.ClearTextOnFocus = false
TeamInput.Active = true
TeamInput.Selectable = true
TeamInput.ZIndex = 1
TeamInput.Parent = Container

-- Team Border
createBorder(TeamInput)

-- Join Button
local JoinButton = Instance.new("TextButton")
JoinButton.Name = "Join"
JoinButton.Size = UDim2.new(0.501400173, 0, 0.166451588, 0)
JoinButton.Position = UDim2.new(0.49970001, 0, 0.750644803, 0)
JoinButton.AnchorPoint = Vector2.new(0.5, 0.5)
JoinButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
JoinButton.BackgroundTransparency = 0.7
JoinButton.BorderSizePixel = 0
JoinButton.Text = ""
JoinButton.AutoButtonColor = false
JoinButton.ZIndex = 2
JoinButton.Parent = Container

-- Join Button Border
createBorder(JoinButton)

-- Join Button Label
local JoinLabel = Instance.new("TextLabel")
JoinLabel.Name = "Label"
JoinLabel.Size = UDim2.new(1, 0, 0.8, 0)
JoinLabel.Position = UDim2.new(0.0088888891, 0, 0.162500367, 0)
JoinLabel.BackgroundTransparency = 1
JoinLabel.Text = "Join"
JoinLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
JoinLabel.TextScaled = true
JoinLabel.Font = Enum.Font.SourceSansSemibold
JoinLabel.TextXAlignment = Enum.TextXAlignment.Center
JoinLabel.TextYAlignment = Enum.TextYAlignment.Center
JoinLabel.ZIndex = 3
JoinLabel.Parent = JoinButton

-- Join Button Gradient
local JoinGradient = Instance.new("UIGradient")
JoinGradient.Name = "UIGradient"
JoinGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 142, 12)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
})
JoinGradient.Rotation = -90
JoinGradient.Parent = JoinButton

-- Uneject Button
local UnejectButton = Instance.new("TextButton")
UnejectButton.Name = "Uninject"
UnejectButton.Size = UDim2.new(0.4, 0, 0.1, 0)
UnejectButton.Position = UDim2.new(0.49970001, 0, 0.911934733, 0)
UnejectButton.AnchorPoint = Vector2.new(0.5, 0.5)
UnejectButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
UnejectButton.BackgroundTransparency = 0.6
UnejectButton.BorderSizePixel = 0
UnejectButton.Text = "Uninject"
UnejectButton.TextColor3 = Color3.fromRGB(255, 255, 255)
UnejectButton.TextScaled = true
UnejectButton.Font = Enum.Font.SourceSansSemibold
UnejectButton.AutoButtonColor = false
UnejectButton.ZIndex = 2
UnejectButton.Parent = Container

-- Uneject Button Border
createBorder(UnejectButton)

--- Draggable Logic ---
local dragging, dragInput, dragStart, startPos
Container.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging, dragStart, startPos = true, input.Position, Container.Position
		input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
	end
end)
UserInputService.InputChanged:Connect(function(input)
	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = input.Position - dragStart
		Container.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- Join Logic
JoinButton.MouseButton1Click:Connect(function()
	local targetTeam = TeamInput.Text
	REMOTE_PATH:InvokeServer(targetTeam)
end)

-- Uneject Logic - Deletes the script and UI
UnejectButton.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
	script.Parent = nil
end)